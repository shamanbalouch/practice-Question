// Q36. T-Shirt: Write a function called make_shirt() that accepts a size and the
// text of a message that should be printed on the shirt. The function should print
// a sentence summarizing the size of the shirt and the message printed on it.
// Call the function.
function make_shirt(text, size) {
    console.log("write message in my shirt is that : ".concat(text, " and  size of shirt is  : ").concat(size));
}
make_shirt("i am a programmer ", 34);
